-- Замок от одновременных заходов воркера. Раньше страница будила воркер,
-- пока задача висела в работе, и заходы накладывались друг на друга: каждый
-- заново слал платный запрос к модели. Теперь по времени последнего старта
-- видно, что заход уже идёт, и повторный вызов отсекается.
ALTER TABLE t_p29007832_virtual_fitting_room.color_guide_tasks
    ADD COLUMN IF NOT EXISTS worker_started_at timestamp without time zone NULL;